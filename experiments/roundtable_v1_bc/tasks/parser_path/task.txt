Implement solve(x) in solution.py using only Python standard library. Return true iff a string is a nonempty slash-separated relative path. Reject absolute paths, backslashes, NUL, empty segments, and segments exactly `.` or `..`. A segment with three dots is allowed. No URL decoding or filesystem access.
