Implement solve(x) in solution.py using only Python standard library. Input [balances, transfers] with nonnegative integer balances and [from,to,amount] transfers. Return a new balances dict after sequential transfers. Unknown accounts, negative amounts or insufficient source funds raise ValueError. Same-account transfers preserve balance but still require sufficient funds. Never mutate input.
