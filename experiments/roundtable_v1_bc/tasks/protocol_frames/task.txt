Implement solve(x) in solution.py using only Python standard library. Parse complete frames from an ASCII string using decimal length:payload framing. Return [payload_list, unconsumed_suffix]. Incomplete header or payload remains unchanged as suffix. Empty input returns [[], ""]. Header must be one or more decimal digits, no sign or whitespace; nondigit header characters raise ValueError even before colon. Zero-length payloads allowed.
