# Deferred

Browser UI, dashboards, SaaS, enterprise features, arbitrary organisations,
extra providers, model economies, learned routing, vector memory, cloud deployment,
multi-user auth, distributed execution, plugin ecosystems, autonomous companies.

Also deferred: automatic dependency installation, large-repository context retrieval,
editing existing acceptance tests, file deletion, hidden-file edits and binary assets.
No work on these until a concrete task needs them.
